# Frontend (apps/web) — variáveis de ambiente (prefixo VITE_ é obrigatório).
# NÃO versione .env / .env.hml / .env.prod.

# URL do backend local.
VITE_API_URL=http://localhost:3333

# Rótulo do ambiente exibido na UI (badge "HML" / "PRODUÇÃO").
VITE_SINQIA_ENV=hml
